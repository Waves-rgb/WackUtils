#include "gtest/gtest.h"
#include "WackUtils/window.h"
#include "../util/shared.h"

// TODO

TEST(Window, findWindows) {
	//ASSERT_FALSE(WackUtils::findWindows([](HWND wnd) { return FILTER_ACCEPT; }).empty());
	//ASSERT_TRUE(WackUtils::findWindows([](HWND wnd) { return FILTER_STOP; }).empty());
	//ASSERT_EQ(WackUtils::findWindows([](HWND wnd) { return FILTER_ACCEPT_STOP; }).size(), 1);
}

TEST(Window, getWindows) {
	//EXPECT_FALSE(WackUtils::getWindows().empty());
}

TEST(Window, getWindowName) {
	//const auto target = createDummy().window;
	//EXPECT_STREQ(WackUtils::getWindowName(target).c_str(), "target");
}